<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.project.mapper.SetMealMapper">

    <insert id="addSetMeal">
        insert into setmeal (category_id, name, price, description, image,status,create_time,create_user,update_user,update_time)
        VALUES (#{categoryId}, #{name}, #{price}, #{description}, #{image},#{status},#{createTime},#{createUser},#{updateUser},#{updateTime})
    </insert>
</mapper>